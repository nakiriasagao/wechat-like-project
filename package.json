{
  "name": "wechat-clone",
  "version": "0.1.0",
  "private": true,
  "description": "类微信聊天软件 — 复刻微信核心体验的即时通讯 (IM) 课程项目",
  "workspaces": [
    "shared",
    "server",
    "web",
    "admin"
  ],
  "scripts": {
    "build:shared": "npm run build -w shared",
    "dev:server": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "dev:admin": "npm run dev -w admin",
    "build": "npm run build:shared && npm run build -w server && npm run build -w web && npm run build -w admin",
    "lint": "npm run lint -w server",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web && npm run typecheck -w admin && npm run typecheck -w shared",
    "test": "npm run test -w server",
    "seed": "npm run seed -w server"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.21.5": true
  }
}
